// import { Box, Container, Typography } from '@mui/material';
import React from 'react';
import { type FC } from 'react';

export const Base: FC<{ title: string }> = ({ title }) => {
  return <div>{'hello'}</div>;
};
